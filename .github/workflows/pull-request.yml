name: Pull Request

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

jobs:
  ci:
    name: CI
    uses: ./.github/workflows/base.yml
    secrets: inherit

  tests:
    name: Tests
    uses: ./.github/workflows/run-tests.yml
    secrets: inherit
